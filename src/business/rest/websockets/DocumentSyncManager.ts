/**
 * Class used to keep client and server document text in sync
 */
export default class DocumentSyncManager {

    constructor(url: string, username: string, password: string, documentId: string) {
    }

}